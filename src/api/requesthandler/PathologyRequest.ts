import { RequestHandler } from 'express'
import UpdatePathologyRequest from '../requests/UpdatePathologyRequest';


export default class PathologyRequestHandler implements RequestHandler<UpdatePathologyRequest> {
  
  constructor() {

  }
  
}